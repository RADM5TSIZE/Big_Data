welche Blcoks auf welchem Rack gespeichert sind. Sollte der Replikationsfaktor nicht mehr gegeben sein so wirde weitere Replikas erzeugt. Der rep. wird je nach wichtigkeit der Datei erzeugt.
Der streaming Zugriff ist optimiert auf stream data access, große Datenmegen.
Man kann auf mehrere Koten gleichzeitig zugreifen.